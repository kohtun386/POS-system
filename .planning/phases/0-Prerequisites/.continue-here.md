# Continue Here

## Session: 2026-06-25

**Phase:** 0 — Prerequisites & Quick Wins
**Branch:** feature/phase-0-prerequisites

## What's Done (0.1, 0.2)

### 0.1 — Settings Role Guard ✅
- `src/App.tsx` lines 93-99: cashier redirected to POS if they try settings

### 0.2 — ErrorBoundary ✅
- Created `src/components/ui/ErrorBoundary.tsx` — class component with `componentDidCatch`, fallback UI, retry button
- Wrapped `<AppContent />` in `src/App.tsx` with `<ErrorBoundary>`

## What's Partially Done (0.3)

### 0.3 — Shop Type + shopsService 🔄
- `src/types/index.ts`: `Shop` interface added (lines 161-169) ✅
- `src/lib/services.ts`: `shopsService` NOT yet added ❌

**Critical Error Fixed:** Accidentally replaced `AppSettings` with `Shop`. Fixed immediately — `AppSettings` restored.

## What's Remaining (0.3-0.6)

### 0.3 — Complete shopsService
- File: `src/lib/services.ts`
- Add `shopsService.getByUserId(userId)` — query `shop_memberships` → `shops`
- Add `mapShopRow()` helper

### 0.4 — Add `state.shop` + `SET_SHOP`
- File: `src/context/SupabaseAppContext.tsx`
- Add `shop: Shop` to AppState
- Add `SET_SHOP` action + reducer
- Update `loadData()` to call `shopsService.getByUserId(user.id)`

### 0.5 — Delete Dead Code
- Delete `src/context/AppContext.tsx`
- Delete `src/components/examples/CurrencyExample.tsx`
- Remove from `package.json`: `react-router-dom`, `@supabase/auth-ui-react`, `@supabase/auth-ui-shared`
- Verify: `npm run lint` passes

### 0.6 — Create shopMembershipsService
- File: `src/lib/services.ts`
- Create `shopMembershipsService.getByUserId()`
- Replace direct `supabase.from('shop_memberships')` in `loadData()`
- Verify: no raw `supabase.from()` calls outside services

## GSD Structure Status

Directories renamed to match GSD convention:
- `.planning/phases/0-Prerequisites/0-Prerequisites-PLAN.md`
- `.planning/phases/1-Feature-Flags/1-Feature-Flags-PLAN.md`
- `.planning/phases/2-Recipe-BOM/2-Recipe-BOM-PLAN.md`
- `.planning/phases/3-Kitchen-KDS/3-Kitchen-KDS-PLAN.md`

**GSD detection still failing** — classifier intermittently blocks bash. May need manual format fix.

## Exit Criteria for Phase 0

- [x] Settings role-guarded
- [x] ErrorBoundary wraps app content
- [x] Shop type added
- [ ] shopsService implemented
- [ ] state.shop populated on login
- [ ] Dead code removed
- [ ] shopMembershipsService replacing raw supabase calls
- [ ] `npm run lint` passes

## Resume

Run: `/gsd-execute-phase 0 --interactive`
Or execute 0.3-0.6 tasks manually.
