# i18n Rules

1. New user-facing strings must use `t()` or `i18next.t()` only.
2. English and Burmese keys must be maintained in pairs at all times.
3. `src/components/platform/` is exempt from i18n scope.
4. SweetAlert and toast messages must also go through `i18next.t()`.
