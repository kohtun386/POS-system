# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.*
!.env.example
supabase-mcp-server-supabase-0.8.2.tgz

# Supabase seed files (contain dev credentials)
supabase/seed.sql
.vercel

# MCP and Claude config (may contain env var references exposing project architecture)
.mcp.json
.claude/settings*.json
.claude/plans/
opencode.json

# Presentations (local use only)
slides/
opencode.json

# Presentation files (local use only)
slides/
opencode.json

# Presentations (local use only)
slides/
.agents/
.codex/
AGENTS.md
*.Zone.Identifier
master-execution-plan.md

# Supabase remote schema dumps (never commit these — they contain insecure grants)
*_remote_schema.sql

# Playwright MCP session logs and test artifacts
.playwright-mcp/
*.png
